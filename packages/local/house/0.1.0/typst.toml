[package]
name = "house"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Josh Murray"]
license = "MIT"
description = "House style: book + factory templates (report/article/minutes/memo/form), components, and PDF/UA-1-clean defaults. Brand colors are themeable."
