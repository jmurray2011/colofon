# Host-side dependency for the document and book factory. The core container uses
# the separately hash-locked requirements-container.txt for repeatable Linux builds.
#   tools/make_doc.py  - Markdown + YAML front-matter -> house template -> PDF/UA-1
PyYAML>=6
